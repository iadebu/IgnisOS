{
  "permissions": {
    "allow": [
      "Bash(if [ -d \"apps/markup-app/src\" ])",
      "Bash(then ls -la apps/markup-app/src/)",
      "Bash(else echo \"La carpeta src/ NO existe en el repositorio local\")",
      "Bash(fi)"
    ]
  }
}
